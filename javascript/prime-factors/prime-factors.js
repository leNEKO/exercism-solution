class primeFactors {
    for(i) {
        for(p of this.sieve(i){

        }
    }

    divmod(i, d) {
        q = Math.floor(i / d);
        r = i % d;
    }

    sieve(i){

    }
}

module.exports = primeFactors;
